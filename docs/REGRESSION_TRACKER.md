# CV 农场助手问题与回归验收台账

更新时间：2026-07-29（Asia/Shanghai）

本台账覆盖本次任务中用户反馈的功能、稳定性、界面和发布问题。状态只根据**失败回归测试、修复后测试、生产部署哈希、实机日志、持久化计数**更新；“进入过流程”不算成功。

## 状态定义

- **已自动验证**：有先失败后通过的回归测试。
- **已实机验证**：生产目录运行日志或持久化计数证明真实动作完成。
- **观察中**：短时证据正常，但仍需长时运行或等待真实场景。
- **待修复/待验证**：尚缺代码修复或实机证据。

## P0：好友顺序、动作与连续处理

| 编号 | 用户反馈 | 当前状态 | 验收标准 | 最新证据 |
| --- | --- | --- | --- | --- |
| F-01 | 从第一位好友开始，但未点“一键务农”就切到第二位 | **v205 已自动+实机验证** | 首位按钮延迟出现时继续等待；快速视觉漏检时补一次原生动作探测；点击并校验消失后才切换 | v205 新回归先失败于原生补探测次数 `0`，修复后为 `1`；实机首行 `row=(365,289)` 后依次出现 `friend visual help click`、`disappeared=True`，之后才 `friend navigation confirmed` |
| F-02 | 多位好友有任务时只处理一位或中途回家 | **v205 已自动+实机验证** | 同一链处理连续多位有动作好友；遇到首个稳定无动作好友后结束 | v205 实机首位完成后切到第二位再次完成帮助，再切到首个无动作好友才结束；摘要 `moves=2 actions=1 exhausted=True`（首位动作在链前单独计数），持久化好友帮助计数增长至 `436` |
| F-03 | 好友列表、拜访、进入农场、点击动作太慢 | **正确性优先已收敛；空链耗时继续优化** | 快速视觉命中立即执行；仅在首位视觉漏检时补一次原生探测；无按钮使用有界等待 | v205 保留视觉快速路径，首位视觉漏检才补一次原生动作探测；生产无动作轮已记录 `friend bounded native action found nothing` 后再切卡，避免直接跳过 |
| F-04 | 同一好友可偷菜又可务农时只偷菜 | **v219 已自动+实机验证** | 同一好友完成偷菜和务农两种动作，动作按钮消失后再切换 | 实机出现偷取完成后保留同一好友再次探测，随后命中 `visual.friend_help_all`，确认按钮消失后才执行底部卡片导航 |
| F-05 | 跳过好友列表第一位，直接进入第二位 | **v223 已自动+截图实证** | 默认永远点第一行；仅第一行封禁才点第二行 | 真实截图中第一行正常、第二行封禁，封禁模板仅命中第二行且 `_handle_friend_list_surface` 点击第一行；新增回归限制第二行失败后不得继续跳第三行 |
| F-06 | 某位暂时未出现按钮时直接跳到下一位 | **v225 已自动+实机验证** | 页面稳定且迟到按钮宽限结束后才可判定无动作；底栏被裁剪但动作按钮可见时仍处理当前好友 | 新增真实脱敏裁剪画面回归先失败后通过；v225 生产重启后实机命中 `visual.friend_help_all`，按钮点击后 `disappeared=True`、好友帮助计数 `750→751`，随后才切换下一张底部卡片 |
| F-07 | 每处理一个好友就回家，再重新打开好友列表 | **v191 已自动+实机验证** | 首次进入后只用农场底部好友卡连续处理 | v191 实机首位完成后连续两次 `friend navigation confirmed` 并各自完成帮助，整链结束后才回家 |
| F-08 | 误点装扮、房屋、外观页 | **已自动验证；实机近期未复现** | 好友链只点击动作按钮、底部好友卡、回家按钮 | v105/v116 后链内禁用好友列表/菜单/装扮回退路径 |
| F-09 | 卡在好友农场/好友列表，日志不断空巡检 | **v223/v224 已收敛** | 明确好友农场由 Hook 当轮接管；列表失败有界重开；封禁跳转回家正常结束 | 生产日志持续出现首行访问、好友动作、底部导航和正常回家；封禁提示条被识别为 `blocked-next-friend-returned-home` 终态 |

## 护主犬与好友名单

| 编号 | 用户反馈 | 当前状态 | 验收标准 | 最新证据 |
| --- | --- | --- | --- | --- |
| G-01 | 只帮护主犬模式仍给所有好友除草 | **已自动+实机验证** | 只有命中已导入好友护主模板的好友才允许帮忙 | 生产持续记录 `guard-list help allowed by native row approval`，每次底部切换后重新匹配当前好友身份 |
| G-02 | 切换好友后继承上一位护主资格 | **已自动验证** | 切换确认后清除旧资格并刷新当前好友模板 | `friend_guard_list_refresh_prequalification` 每位执行一次 |
| G-03 | 对列表逐行重复识别，速度慢 | **已改善** | 当前页面批量匹配模板，选择首个合格行；进入后不再回列表逐行扫 | 实机一次输出 5 行 `guard_scores` 并直接点击首行 |
| G-04 | 导入模板时把本人绿色头像当候选 | **已自动验证** | 本人头像不得进入模板候选 | `test_bottom_fixed_self_profile_is_not_a_guard_candidate` 覆盖底部本人卡片排除 |
| G-05 | 需要逐个勾选，缺少全选和一次保存 | **已自动验证** | 支持全选/取消全选/一次确认保存 | 批量选择跳过隐藏、禁用和本人行，并由一次保存调用原生保存按钮 |

## 自动捣乱

| 编号 | 用户反馈 | 当前状态 | 验收标准 | 最新证据 |
| --- | --- | --- | --- | --- |
| T-01 | 自动捣乱一次也没有执行 | **v200/v205 已实机验证成功** | 真实计数增加且持久化 | v205 实机通过几何种植证据与弹层动作兜底完成 `142->157`，持久化 `friend_trouble_daily_count=157`；原生返回值仍不作为成功依据 |
| T-02 | 无可捣乱目标时在多个好友间停留过久 | **v224 已自动验证并部署** | 找不到 trouble 时采用有界快速退出/冷却，不长期占用好友链 | 默认最多检查当前好友和 2 位相邻好友（共 3 位），完全未命中后进入 90 秒冷却；显式配置仍可覆盖上限 |
| T-03 | 原生函数返回 True，但实际没有计数增长 | **已防误报** | 只有持久化计数增加才记为成功 | v150 以 `before_count -> after_count` 为权威；`91->91` 判定失败并继续 |

## 每日分享与每日任务

| 编号 | 用户反馈 | 当前状态 | 验收标准 | 最新证据 |
| --- | --- | --- | --- | --- |
| S-01 | 输入 QQ 后未点联系人头像左侧圆圈，发送按钮灰色 | **v1.4.8 已自动+实机验证** | 搜索唯一联系人，点击选择圆圈，确认按钮可用后直接发送 | 实机完成精确目标输入回读、`selected_count=1`、直接发送点击和对话框关闭，状态写入 `verified-direct-contact-send-v2` |
| S-02 | 误建群聊、创建多个群组 | **v1.4.8 已自动+实机验证** | 只走单联系人直接发送，不进入群聊创建入口 | 实机证据为单目标 `selected_count=1` 与 `confirm=direct-uia`，没有进入群聊创建链 |
| S-03 | 分享成功后当日仍重复 1/6–6/6 | **v1.4.8 持久化证据通过** | 只有完整发送证据才记录成功，成功后同日不重复 | `daily_flow_status.json` 写入 `status=success`、`reason=verified-direct-contact-send-v2`、精确目标和校验时间；迟到原生失败日志会改写为成功确认 |
| S-04 | 昵称/QQ 目标不可见或不可编辑 | **已自动验证** | 每实例可编辑并展示当前定向分享目标 | 设置注入和持久化回归覆盖目标读取、保存、清空和实例级配置 |
| D-01 | 每日任务已领取仍识别失败并循环打开 | **持久化状态通过** | 当天权威完成后不再重试；失败有重试上限和退避 | 四份计数文件 `task_last_date=2026-07-29`，v186 重启后未循环打开任务 |
| D-02 | 红点误判覆盖已经成功的任务/分享状态 | **已自动验证** | 红点仅触发待处理，不能覆盖持久化成功日期 | v137 日状态权威回归 |
| D-03 | 重启机器人/软件后重复领取或分享 | **v186 重启实机通过** | 重启后恢复当日完成状态 | 10:41:48 重启后未见任务/分享重复流程 |
| D-04 | 商城每日免费福利已领取/限购用完后仍循环打开，并报 `maximum recursion depth exceeded` | **v230 已自动+实机验证** | 周期扫描不增加包装层；递归异常先进入持久化退避，不再每隔数秒重复打开 | 新进程 PID 76608 加载 v230；未来日期日志标记 `2026-07-30 00:44:22` 记录福利完成，之后 3 分钟内递归错误 0，分享入口包装与每日状态包装均只安装 1 次 |

## 自家农场、播种、权益与计数

| 编号 | 用户反馈 | 当前状态 | 验收标准 | 最新证据 |
| --- | --- | --- | --- | --- |
| H-01 | 长时间只检查好友，不回自家收获 | **已自动+近期实机验证** | 好友链结束后回家并恢复自家巡检 | v223/v224 好友链结束后恢复正常间隔，生产日志持续自家/好友交替并执行自家一键务农 |
| H-02 | 成熟一小时仍未收获，重启才识别 | **状态锁修复已自动验证** | 好友状态不锁死自家分支；成熟按钮出现后无需重启即可处理 | 自家画面上的好友误判、回家门禁和强制自家复核均有回归；生产已多次在未重启情况下重新进入自家维护 |
| P-01 | 检测到空地但不播种、选种等待很久 | **v1.4.8 自动回归通过；真实 2×2 场景观察中** | 自家空地未清零时禁止好友路线；部分库存先种；2×2 只认相邻田字型并点击对号；失败后普通种子继续 | 4×6 的全部 15 个局部 2×2 位置均命中；0–3 块或无合法田字型时原生特殊种子调用为 0；确认失败后重新扫描普通种子；自家空地需要连续两次为 0 才放行好友巡检 |
| V-01 | 日志偶尔显示“当前非 VIP” | **已自动+近期实机验证** | 任务上下文和业务函数始终读取同一权益状态 | v224 启动后持续加载统一权益上下文，生产未出现“当前非 VIP”；6 分钟稳定采样无重启、无无响应 |
| C-01 | 界面计数与日志/持久化不一致 | **核心计数已同步；场景字段仍有瞬时差异** | 四份持久化文件核心计数一致，GUI 从同一权威源刷新 | 2026-07-29 15:54：实例权威计数 `help=436, trouble=157, radish=60, self=135, task/share=2026-07-29`；动作增长均由持久化文件复核 |
| C-02 | 萝卜专项计数被通用播种次数污染 | **v182 已自动+实机验证** | 专项计数只读专项字段 | `daily_radish_exp_count=60`，通用自家计数可独立增长到 69 |

## 稳定性、性能、界面与交付

| 编号 | 用户反馈 | 当前状态 | 验收标准 | 最新证据 |
| --- | --- | --- | --- | --- |
| R-01 | 运行约一小时后闪退/自动退出 | **监督恢复已修复；稳定采样通过** | 异常退出有记录并自动拉起；最终构建持续响应 | v206 连续 30 分钟 60 个样本无中断；v223 连续 12 分钟 72 个样本无异常；v225 连续 4 分钟 24 个样本 PID=30400、缺失 0、无响应 0、工作集 416.9–448.5 MB |
| R-02 | CPU 满载导致鼠标、视频、声音卡顿 | **v208 已修复并复核** | OpenCV/OCR 线程受限；好友链结束恢复正常巡检间隔 | v223 稳定采样工作集 373–437 MB、无无响应；快速好友间隔在统一收敛函数恢复为原配置 |
| R-03 | 多余 PowerShell 长期驻留 | **当前未发现** | 部署脚本结束后不残留项目相关 PowerShell | v186 部署完成后仅检测到当前检查命令自身 |
| U-01 | 深色主题覆盖导致按钮截断、状态栏错位、双击无反应 | **已回退原生浅色布局并自动验证** | 控件完整、状态栏对齐、双击启动正常 | 生产保持原生浅色布局；主题注入为空，启动器与控件回归通过 |
| U-02 | 关于页包含旧作者、群聊、联系方式、推广网址 | **v216 已自动验证** | 第一次点击即只显示本项目说明和本人 GitHub 信息 | 问号按钮在首帧前断开原生槽并连接 `projectInfoDialog`；重复补丁仍只触发新项目对话框 |
| U-03 | README 图片模糊、头像未脱敏、项目介绍不足 | **v1.4.4 发布资料已更新** | 高清脱敏截图、完整功能介绍、更新记录和效果图 | README 版本、测试数、版本表和 CHANGELOG 已更新为 v1.4.4 |
| REL-01 | GitHub 只有一个安装包，看不到迭代历史 | **v1.4.8 发布收尾中** | 独立 commit、tag、CHANGELOG、Release 和安装包 | v1.4.8 已更新版本文件、README 与 CHANGELOG；发布完成后记录 commit、tag、Release、ZIP 与 SHA-256 |
| FS-01 | 项目与构建缓存占用 C 盘 | **源码、生产与发布均以 E 盘为主** | 构建和发布产物放在 E 盘；迁移后启动和 Git 正常 | 源码位于 `E:\CodexProjects`，生产程序位于 `E:\CV农场助手`，构建输出固定为 `E:\CodexBuilds\qq-farm\releases`；C 盘仅保留主程序固定活动配置/日志 |

## 本轮 v231–v244 / v1.4.8 收尾证据

1. **配置不再被更新流程重置**：启动器整文件通过 Windows PowerShell 解析；活动 `config-multi.ini` 启动前后 SHA-256 一致，`.bad-*` 文件数量没有增加。编码处理只删除 UTF-8 BOM 三个字节，不解析和重排任何配置项。
2. **自家农场优先**：空地出现后持续阻断好友路线，播种后跳过冷却立即复核；只有连续两次确认空地为 0，好友巡检才重新放行。部分白萝卜库存先种，模板漏检优先执行原生铲子/拖拽，再进入买种判断。
3. **2×2 与普通种子降级**：4×6 土地的 15 个相邻田字型全部进入候选；0–3 块、无合法田字型、确认失败和连续事务失败均只关闭当轮特殊种子，普通种子继续处理剩余空地。
4. **等级策略**：首次调用强制实时 OCR；`120/121 + 活动配置 125` 修正为 125，`121 + 旧配置 40` 保持 121，不发生 40 级回退。
5. **分享证据闭环**：只有目标匹配、单联系人选择、直接确认和对话框关闭四项全部成立才持久化成功；原生恢复调用根据函数签名传入当前 `game_frame`；成功后的迟到失败日志改写为明确成功提示。
6. **单个收获衔接播种**：真实“单个收获连续点击完成”日志会增加事件令牌并立即追加播种配额；历史乱码哨兵已由红绿回归替换。
7. **好友与设置**：首位、护主卡、偷取后务农、封禁第二行、无动作好友继续扫描、装扮门禁均保留回归；运行时工作流恢复不写回休息时段、周期重启或其他业务设置。
8. **自动验证**：完整 `481 tests / OK`；发布打包回归确认 ZIP 含 `VERSION`、`README.md` 和 `版本与更新日志.md`；`py_compile`、PowerShell 解析、启动器 BOM 检查和 `git diff --check` 通过。
9. **时间说明**：当前会话日期为 `2026-07-29`；电脑日志出现 `2026-07-30` 未来时间戳，本次保持用户系统时间不变，只将日志内容作为动作证据。
10. **现场限制**：当前真实画面没有可用于最终 2×2 验收的四块相邻空地；该项保持“自动回归通过、等待真实田字型现场”的证据分级，不把普通播种成功当作 2×2 成功。
## 本轮 v186 变更与验证

1. 新增失败回归：护主名单首位好友的“一键务农”在第 11 次快速探测才出现时，禁止提前切换。
2. 修复：护主名单首位默认渲染宽限从 8 次提升到至少 12 次，仍保持模板快速探测，不恢复原生慢扫描。
3. 自动测试：`python -m unittest discover -s tests -p "test*.py"` → **363 tests / OK**。
4. 静态验证：`python -m py_compile portable\hook.py`、`git diff --check` → 退出码 0。
5. 部署：源文件与 `E:\CV农场助手\hook.py` SHA-256 一致，生产进程已重启。
6. 实机动作：10:42:23 首位好友帮忙按钮点击成功、按钮消失校验成功，之后才切换下一位；好友帮忙计数增加至 170。

## 本轮 v189–v191 变更与验证

1. **v189 好友列表入场屏障**：点击首行后记录 pending，好友界面未准备好时禁止导航和回家；2.8 秒稳定窗、8 秒超时释放。
2. **v190 可见动作提前放行**：若首位“一键务农/偷菜”按钮已经可见，不再等待固定稳定窗；生产实机最短在进入后 `0.866s` 开始点击。
3. **v191 迟到按钮宽限**：好友列表首位使用一次性扩展宽限，最迟覆盖第 20 次快速探测；切换下一位后至少覆盖 24 次探测，按钮出现即立即执行。
4. **TDD 证据**：新增回归先分别失败为 `12 < 20` 与 `16 < 20`，最小修复后转绿；兼容回归一度暴露旧用例只移动 1 位，改为“仅好友列表入场启用扩展宽限”后恢复。
5. **自动验证**：定向 8 项通过；好友核心 `240 tests / OK`；完整 `373 tests / OK`；`py_compile` 与 `git diff --check` 退出码 0。
6. **生产部署**：`v191-delayed-friend-action-grace` 已部署；源码与 `E:\CV农场助手\hook.py` SHA-256 均为 `897468EC4A89621ADBA18EA111A1922052406F298AD4F243F126018A41E54AF5`；护主模板保持 29 张。
7. **实机连续好友链**：已两次观察到首位动作点击并校验消失后，连续切换两位好友并再次完成帮助；摘要均为 `moves=3 actions=2`（初始动作另计），持久化好友帮助计数持续增长至 242，随后回家验证成功。
8. **进程状态**：生产 PID `63776`，启动时间 `2026-07-29 12:12:43`，短时 `Responding=True`；部署脚本结束后未发现项目相关常驻 PowerShell。

## 本轮 v225 收尾证据

1. **好友动作场景门禁**：真实脱敏裁剪画面缺少底部绿色选中框，但“回家”与“一键务农”清晰可见；新增回归修复前返回普通页面，修复后返回好友农场。
2. **2×2 特殊种子事务**：原生 `_try_plant_quad_act_seeds` 已直接纳入补丁，按“连续四格空地→点击特殊种子→对号确认”执行；首次瞬时漏检只重试同一事务一次，并恢复原阈值。
3. **自动与实机验证**：专项 294 项、完整 432 项通过；生产好友帮助计数 `750→751`，按钮消失校验后才切换下一位。
4. **部署与稳定性**：源码和生产 `hook.py` SHA-256 一致；PID `30400` 连续 4 分钟 24 样本一致，缺失 0、无响应 0，工作集 416.9–448.5 MB，线程 60–69。
## 发布前统一验收规则

1. 每个行为修复先写失败测试并确认失败原因正确，再实施最小修复。
2. 每轮改动运行目标测试；准备发布时运行完整测试、编译、差异检查和构建启动验证。
3. 生产部署后核对源码/生产文件 SHA-256，并从新 marker 后提取日志，旧日志不作为新版本证据。
4. 分享、任务、捣乱、播种以持久化日期/计数或可见结果为准，函数返回值不单独作为成功依据。
5. 好友动作必须满足顺序：当前好友偷菜/务农完成并校验 → 底部下一张卡 → 新好友重新确认身份与动作。
6. 当前实际日期统一为 **2026-07-29**。

## 本轮 v193–v194 变更与验证

1. **v193 安全播种诊断**：新增失败回归证明 inventory 会调用 `dis.dis`；移除受保护 callable 的字节码反汇编，仅保留 `signature/co_varnames/co_names/co_consts` 等安全元数据。
2. **v194 首位好友预认证保留**：实机日志证明原因为“好友入场 pending 期间，原生链临时清除 chain pending，导致护主名单预认证被提前清空”。修复后，`friend_chain_pending` 或 `friend_entry_pending` 任一成立时都保留首位认证。
3. **TDD 证据**：新回归分别先失败于 `dis.dis` 被调用，以及 pending 入场时认证被清空；最小修复后均转绿。
4. **自动验证**：好友核心 `243 tests / OK`；完整 `376 tests / OK`；`py_compile` 和 `git diff --check` 退出码 0。
5. **生产部署**：`v194-pending-entry-guard-approval` 已部署；源码与 `E:\CV农场助手\hook.py` SHA-256 均为 `0B750770FB69BBAB0A6DB87FADA247EF137343AF7317D85CDF0CC18639281DD5`，生产 PID `68388`。
6. **首位实机证据**：新 marker 后立即出现首行 `v117 friend list visit` → `v147 preserved` → `v190 ... age=1.194` → `v142 guard-list help allowed` → `v70 ... disappeared=True`，随后才 `v120 friend navigation confirmed`。
7. **连续好友实机证据**：同一链中首位成功后继续切到第二位并再次完成一键务农；好友帮助持久化计数由 `256` 增长到至少 `269`。
8. **崩溃观察**：`fatal_fault_trace.log` 只有 53 字节的本次 `process_start` 头，未出现 access violation；进程 `Responding=True`。
## 本轮 v195–v198 变更与验证

1. **v195 面板等待压缩**：将背包优先分支 `panel_settle` 从 `1.500s` 限制到 `0.350s`；实机仍为 `29.455s`，证明固定等待不是主要瓶颈。
2. **v196 安全性能画像**：仅在背包优先分支记录 helper 耗时，不反汇编受保护 callable。实机定位 `_check_empty_land_label_with_retry` 单次 `30.206s`，为当时直接瓶颈。
3. **v197 已验证跳过重复土地名称 OCR**：新增回归 `test_backpack_preverified_empty_land_skips_slow_label_ocr`，先因包装器不存在失败，再以最小包装器转绿；仅对已经由 `_detect_empty_lands` 认证且坐标有效的背包分支返回预认证结果，普通调用保留原链。
4. **v197 自动验证**：好友核心 `246 tests / OK`；完整 `379 tests / OK`；`py_compile` 与 `git diff --check` 退出码 0。
5. **v197 生产部署**：源码与 `E:\CV农场助手\hook.py` SHA-256 均为 `F8EA849E6BDEA4B9D386D087FDC80A6039C6D61BA017139779F336EAE7C7EFD3`。实机出现 `v197 backpack preverified empty land; skipped slow label OCR`。
6. **v197 历史播种基线**：`13:21:31.662` 检测到 1 块空地，`13:22:04.555` 完成 `背包优先#2 x 1`，`13:22:07.824` 确认覆盖全部空地；当时总耗时 `36.161s`。该历史结论已由 v218–v221 的背包库存门禁、快速数量识别和作物覆盖空地过滤继续收敛。
7. **好友首位与连续处理的新鲜实机复核**：v197 marker 后出现首行 `friend list visit row=(365,289)`，随后 `guard-list help allowed`、点击成功、按钮消失校验成功；同链继续切换并再次完成两位好友，摘要 `moves=3 actions=2`（首位动作另计）。好友帮助计数从 `295` 增长到至少 `302`。
8. **v198 自动捣乱根因**：真实调试帧证明旧几何回退在全空地画面返回 11 个空地中心，在密集成熟作物画面只返回 4 个右侧景物/边缘点，导致点击后没有弹层，原生函数返回 `True` 但持久化计数保持 `112`。
9. **v198 TDD 修复**：新增 `test_troublemaker_dense_crop_field_returns_crop_centers_not_right_edge_artifacts` 与 `test_troublemaker_empty_soil_is_not_treated_as_planted_land`；修复前分别失败为 `4 < 12` 和空地错误返回 11 点。修复后以局部绿色种植证据过滤空地，以密集作物区域构建 15 点拖拽格，空地画面返回空列表。
10. **v198 自动验证与部署**：好友核心 `248 tests / OK`；完整 `381 tests / OK`；`py_compile` 与 `git diff --check` 退出码 0。生产源码哈希 `84CD2019CF5E592426C4A7DEA37A1419A72D320AB7305E3C48F9F2937C749419`，进程 `PID 70468`，启动时 `Responding=True`。自动捣乱仍须以 `friend_trouble_daily_count > 112` 或等价持久化计数增长作为最终实机验收。
## 本轮 v199–v205 变更与验证

1. **v199 捣乱种植证据门禁**：原生 `seed_land` 候选不再单独作为可捣乱依据；真实帧必须存在已种植区域证据，无作物空地拒绝进入捣乱点击。
2. **v200 捣乱弹层动作兜底**：当原生弹层按钮识别缺失时，使用双按钮几何关系选择动作按钮；后续生产持久化计数由 `112` 增长至 `120`，v205 又完成 `142->157`。
3. **v201 性能试验与回归暴露**：跳过 no-seed OCR 虽缩短流程，但背包数量徽章 OCR 偶发空结果会回退慢路径并导致漏种，因此该优化被判定不满足正确性门槛。
4. **v202 恢复原生 no-seed OCR**：新增失败回归保护原生返回值；生产真实完成 1 块空地播种，`14:41:30` 检测空地、`14:41:49` 完成 `背包优先#2 x1`，整段约 `22.099s`。
5. **v203 好友列表进度游标**：新增两条失败回归，覆盖“列表总从第一行开始”和“底部切卡后列表游标不前进”；生产行选择从 `289 -> 478 -> 667` 持续推进。
6. **v204 背包种子面板捕获**：新增 `backpack-seed-panel-live.png` 捕获链和失败回归；源码/生产哈希一致后已部署，等待下一次自然播种触发真实面板。
7. **v205 首位好友视觉漏检补探测**：新增回归 `test_guard_list_first_friend_runs_native_probe_before_advancing_after_visual_miss`，旧代码失败为原生探测 `0` 次；最小修复后，好友护主列表模式仅在首位快速视觉漏检时补一次原生动作探测。
8. **v205 自动验证**：定向 6 tests、好友核心 `257 tests / OK`、完整 `390 tests / OK`；`py_compile` 与 `git diff --check` 通过。
9. **v205 生产部署**：源码与 `E:\CV农场助手\hook.py` SHA-256 均为 `B46FF6091ACE219981E70759FBF6E905BBBBBE1071FDF4BB7CEDCC501D61FF01`；PID `71540`，部署时 `Responding=True`。
10. **v205 好友实机顺序**：首轮从 `row=(365,289) cursor=0/5` 进入；首位 `friend visual help click -> disappeared=True`，随后才切卡；第二位再次 `friend visual help click -> disappeared=True`，再切到首个无动作好友结束。好友计数增长至 `436`。
11. **v205 无动作防跳过证据**：快速视觉未命中时生产日志出现 `friend bounded native action found nothing`，之后才允许切到相邻好友，已消除“视觉一次漏检即直接跳过”的路径。
12. **v205 自动捣乱实机**：生产日志记录 `attempt=2/5 raw=True count=142->157 success=True`，最终状态由持久化计数确认。
13. **v206 列表点击确认前不推进**：生产日志曾连续出现 `row=(365,572)` 后立刻 `row=(365,667)`，证明首次点击未进入农场时游标已被提前推进。新失败回归重现旧序列 `288→383→288→383`；修复后同一行有界重试 3 次，序列为 `288→288→288→383`，避免点击丢失时跳过好友。
14. **v206 自动验证与部署**：好友核心 `258 tests / OK`、完整 `391 tests / OK`、`py_compile` 与 `git diff --check` 通过；生产源码 SHA-256 `43FAC7B081E2B8E35E4181DFED957A974E469268964EBE39D50D51FD25B98A9D`，PID `66708`，部署时响应正常。
15. **v206 新鲜实机链**：新 marker 后从 `row=(365,288) cursor=0/5` 进入，首位帮助按钮点击并 `disappeared=True`，底部导航确认后第二位再次点击并消失，随后才在首个无动作好友结束；持久化好友帮助计数增长至 `450`。
16. **v206 半小时稳定性实测**：`2026-07-29 16:11:36–16:41:16` 共 60 个 30 秒样本，PID 始终为 `66708`，`RespondingFalse=0`；工作集 `394.1–474.3 MB`，私有内存 `343.8–451.5 MB`，线程 `59–70`，句柄 `1753–1830`，未发生进程中断。CPU 平均约 `0.476` 个逻辑核心，峰值约 `2.333` 个逻辑核心；峰值时段与播种/OCR 和高频好友巡检重叠。
17. **v207 未确认列表点击不推进**：新增失败回归证明 v206 在同一行 3 次点击仍未进入农场后会推进到下一行；v207 将列表游标提交延后到好友农场界面可见确认时，未确认点击始终保留原行，并在 3 次快速尝试后进入 `1.5s` 退避，避免跳过好友和高频点击。好友核心 `259 tests / OK`、完整 `392 tests / OK`；生产哈希 `D1512202727DC64286CBFC79653E47DDE15B8D585379668FD6A1BFC957592BD7`。实机出现 `v207 friend list confirmed cursor pending=9 next=10` 后才执行帮助，按钮连续 3 次点击最终 `disappeared=True`，好友帮助计数增长至 `495`。
18. **v208 好友链结束恢复正常巡检间隔**：稳定性日志显示好友链启用 `0.75s` 快速间隔后，统一收敛函数只清状态却未调用间隔恢复，造成回家后仍高频输出“开始/结束巡检”。新增失败回归 `test_friend_chain_finalize_restores_original_check_interval`，旧行为保持 `0.75`、修复后恢复原始 `15.0` 并移除临时字段。好友核心 `260 tests / OK`、完整 `393 tests / OK`；生产源码与 `E:\CV农场助手\hook.py` SHA-256 均为 `114E06D9BC2D185F348CB2A3B19A38E9FB4365C5078F3A155191B601BA63DD0E`，PID `42304`，部署时响应正常。

19. **v209 未确认行超时重开列表且保留游标**：新增失败回归 `test_pending_friend_row_reopens_list_after_entry_timeout_without_advancing`，覆盖同一好友行点击持续未进入农场时无限原地重试的问题。修复后同一行进入等待超过 `6s` 会关闭好友列表、清理 pending、恢复正常巡检间隔，但保留当前游标；下一次重新打开列表仍从该好友重试。生产日志标记为 `v209 friend list pending row reopen recovery`。
20. **v210 持久化每日帮助上限门禁**：生产持久化计数曾达到 `513/500`，证明视觉帮助路径绕开原有上限。新增失败回归 `test_visual_friend_help_stops_at_durable_daily_quota`，从运行时与 `daily_counters.json` 读取同日最大计数；达到上限后不再点击“一键务农”。生产部署后 `friend_help_daily_count` 保持 `513`，未继续增长；历史超出的 `13` 次不篡改，次日按日期自然重置。
21. **v211 上限后短路好友链并继续收敛捣乱/回家**：新增失败回归 `test_friend_continuation_stops_before_navigation_when_help_quota_exhausted` 与 `test_friend_home_recovery_skips_entry_when_help_quota_exhausted`。修复后达到帮助上限时好友 continuation 直接以 `reason=friend-help-quota-exhausted, moves=0` 结束，后续好友入口恢复路径跳过，门禁日志按 `30s` 节流且跨日自动失效。自动验证为好友核心 `264 tests / OK`、完整 `397 tests / OK`、`py_compile` 与 `git diff --check` 通过；源码/生产 SHA-256 均为 `C964F2D644AB1BEC2C4A5C0DA2C6ABC8FDC7E82CB0EC59804A2E92F56AE2A45C`。生产 PID `62304`；新 marker 后自动捣乱持久化计数 `186→192`，随后 `troublemaker success finalized friend chain`、回家点击与页面切换校验均成功。
## 本轮 v219–v224 收尾证据

1. **好友首行与封禁规则**：新开列表重置历史游标；第一行封禁才选第二行；第二行失败后关闭重开，不再继续跳第三行。用户真实截图离线匹配结果为第一行 `False`、第二行 `True (0.9512)`，实际点击第一行。
2. **封禁相邻好友回家**：好友农场底部切换后若出现封禁提示并被送回家，链路以 `blocked-next-friend-returned-home` 正常结束。
3. **好友动作顺序**：偷取完成后保留同一好友进行一次帮助复核；帮助按钮消失后才切换底部相邻好友。
4. **播种误报过滤**：已种植地块中心周围的作物绿色覆盖用于过滤裸土误报，生产从 3 个错误空地进入 79 秒背包链，收敛为约 6 秒结束。
5. **自动捣乱**：成功只认持久化计数增长；默认完全未命中扫描限制为 3 位好友，随后 90 秒冷却。
6. **完整验证**：429 项测试通过；`py_compile`、`git diff --check` 通过；v223 12 分钟 72 个稳定性样本 PID 一致、缺失 0、无响应 0。

## 本轮 v226–v229 收尾证据

1. **三块空地降级**：0–3 块空地不调用 2×2 原生事务，同一轮继续普通单格种子。
2. **24 块土地全枚举**：`_qqfarm_find_all_quad_empty_land_groups` 遍历所有空地四点组合，4×6 合成农场的 15 个局部 2×2 全部命中，中间田字型可用，整片农场外侧四角与跨格组合被排除。
3. **2×2 失败后继续**：未找到合法田字型或确认事务失败两次时，特殊种子仅在当轮跳过，普通种子重新扫描。
4. **好友误报抑制**：同一底部好友卡的务农按钮在 3 次点击后仍不消失时，记录卡片边界并抑制 15 秒，消除一位好友数分钟重复三击的卡住路径。
5. **首位护主好友复核**：列表预认证标记丢失时，直接用当前选中轮播卡与已导入护主模板匹配；实机日志出现 `v229 guard-list help allowed by current carousel identity`。
6. **中间无动作好友不再终止链**：护主列表模式默认有界向后检查 6 个底部卡；实机已在 `move=2/6` 的无动作好友后继续，随后链路总结为 `moves=3, actions=1`。
7. **实机收敛**：后续底部切换遇封禁好友并被游戏送回家，以 `blocked-next-friend-returned-home` 正常结束，没有重进好友列表或装扮页。
8. **自动验证**：好友/播种核心 302 项通过，完整回归 440 项通过；`py_compile` 与 `git diff --check` 通过。
9. **未完成的实机证据**：当前只有 3 块空地，真实 2×2 特殊种子的对号确认与四块空地减少证据继续等待四块相邻田字型空地现场。
