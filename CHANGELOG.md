# 更新日志

## [1.4.39] - 2026-08-10

### 每日福利、分享与会员状态
- 免费福利达到同日失败上限后，在原生 `should_run_daily_freebenefits` 和 `run_daily_freebenefits` 两个入口同时硬停止；商城红点、通用模板和坐标兜底均不再突破封顶。
- 用户明确确认已人工领取时，保存独立的 `user-confirmed-manual-claim-v1` 证明，不冒充自动领取，也不在启动修复时降级为待执行。
- 精确联系人发送与分享奖励拆成两个同日事务；已验证发送阻止重复分享，奖励页误点产生的联系人窗口只关闭，不再次发送。
- 顶部“已激活”按钮改为本地会员功能状态说明入口，明确显示本地状态与业务开关关系，不进入购买、续费或卡密页面。

### 发布与隐私
- README 明确声明项目永久免费，维护者不会向任何用户收取下载费、会员费、卡密费、激活费或售后费。
- 发布包排除 `UserData`、日志、备份、现场截图、诊断目录、并行实验运行时、旧快捷方式和机器专用路径。
- 仓库示例统一使用占位联系人，公开源码和 Release 不包含用户 QQ 号、个人配置、计数、状态或日志。

### 验证
- 每日状态、免费福利、分享、奖励和包装幂等：68 / 68 OK。
- 每日可见性、指标同步、原生入口恢复和定向分享：107 / 107 OK。
- 重启后 9 轮巡检：免费福利点击 0、重试计数增长 0、Traceback/ERROR 0。

## [1.4.38] - 2026-08-09

### 原生业务执行路径与稳定接管
- 统一原生 v2.2.5 业务执行所有权，避免新旧包装同时点击同一入口；好友帮助、自动捣乱、自动施肥和每日流程使用同一调度与计数桥接。
- 好友帮助使用按钮变化、计数变化和页面转场等强证明，同一好友与同一动作按日去重；末行、返回自家和好友列表恢复均保持有界。
- 物理 PrintWindow/WGC 捕获增加来源标记和遮挡仲裁，满地观察与可点击坐标权限分离，防止仅凭模板数量生成种植动作。
- 便携启动器保持单工作进程、隐藏启动、崩溃监督和用户配置迁移；正式目录与源码按哈希部署。

## [1.4.37] - 2026-08-06

### 固定 24 格土地账本
- 以 L01–L24 固定坐标账本代替模板命中数量，状态区分 `empty`、`occupied-1x1`、`occupied-2x2` 和 `unknown`。
- 1×1 仅提交目标单格由空变占用；2×2 仅提交同一完整田字格四格由空变占用，拒绝 delta=5、跨事务变化和遮罩画面提交。
- 每次动作后关闭种子架、土地标签和 2×2 覆盖层，重新对齐相机并抓取两张新鲜同状态画面；120 秒到达后保存 pending 槽位并结束本轮。
- 金土地、紫土地只作为土地品质和视觉材质处理，不改变 1×1/2×2 事务规则。

## [1.4.36] - 2026-08-05

### 满地好友求助、收获后单格与好友末行恢复
- 首页为真实自家满地且出现“好友求助”时，只有新鲜可见 QQ 画面和精确文字模板命中才能打破旧的好友空巡检锁；解锁后仍保持 scene_hint=home，直到好友入口完成真实转场，避免假“已进入”和同一气泡热循环。
- 好友画面误入 process_self_farm 时改走好友偷菜/帮助链；好友页“一键务农”不再建立自家补种状态或写入自家维护计数。
- 单个收获与一键务农统一进入 post-harvest snapshot；收获后确认的真实空地不会因一次土地标签识别失败被隔离 900 秒。
- 同中心的普通候选与 post-harvest confirmed 候选合并时保留 _qqfarm_post_harvest_confirmed、_qqfarm_visual_soil_proof 和更高置信度，避免 confirmed 证据因 first-wins 去重丢失。
- _qqfarm_post_harvest_pending 直接阻止稳定满地 fast-skip、terminal-zero 清场和 home-priority 释放；队列真正消费完以前持续保留自家优先。
- 实机发现 resume cursor=4/5 在重新打开好友列表时被“新会话清游标”重置为 0；现在 resume_pending=True && exhausted=False 被认定为同一轮明确恢复，直接访问第五行。只有 exhausted/stale 的新会话才从第一行开始。

### TDD、回归与实机证据
- test_live_home_request_and_single_harvest_p0_20260805.py：11 / 11 OK。
- test_live_friend_selected_second_reconcile_p0_20260805.py：6 / 6 OK，同时证明“旧第二位状态的新会话从 0 开始”和“同轮 4/5 恢复不被重置”。
- 末行受影响组合 361 / 361 OK；好友专项 416 / 416 OK；种植/空地/背包专项 269 / 269 OK；完整回归 1042 / 1042 OK。
- 最终隔离性能 120 / 120：Responding=False=0，单核均值 41.15%，最大工作集 515084288，最大线程 89，source/isolated 一致、正式配置不变、进程树停止、failure=null。
- 最终正式稳定 120 / 120：PID 11436，Responding=False=0，单核均值 33.72%，最大工作集 515940352，最大线程 89；Traceback=0、PrintWindow timeout=0、旧好友锁热循环 0、单格 (185,454) 隔离 0。
- 同一正式观察段出现 resume cursor=4/5 → resuming saved cursor=4/5 → visit cursor=4/5，并且 reset stale cursor=4 pending=4 为 0，末行恢复已形成实机闭环。

### 正式身份与回滚
- VERSION 1.4.36；source/deploy hook 44A97C96B29D80AEAC37EBD0B2A0D0C1C59EA042E12B80678705309C71DD3321 / 1758643 bytes；无 BOM。
- 好友求助文字模板 SHA-256 58EF867C4354B9D1CD8FB1D022A2936BC59D5657C9767001679C9B53BF3E7D45。
- 正式配置 SHA-256 保持 5A63582C4A6A9153B51E819DC9D645C44997FD59AA8FE9E0C4A54A5DCA3762A4；UserData 仍为 570 个文件。
- 基础回滚：E:\CV农场助手\backups\v359-perfect-1.4.36-20260805；末行最终补丁回滚：E:\CV农场助手\backups\v359-final-last-row-1.4.36-20260805。
- 当前自家画面已满地，因此随机局部收获的 20–60 秒（硬上限 120 秒）现场签收继续等待用户下一次制造少量可收获地块；该自然机会不阻塞代码、测试、性能、备份和部署。

## [1.4.35] - 2026-08-05

### 好友第二位错位、动作强证明与页面状态
- 将用户截图固化为 `tests/fixtures/live-friend-selected-second-iris-20260805.png`：真实画面处于 `iris / 104` 好友农场，第二张好友卡被选中，而第一张等级 108 的好友仍在左侧可见；该证据同时覆盖旧游标跳过第一位、帮助假成功、`scene_hint=home` 与真实好友页不一致、停在好友页空轮询。
- 新好友列表会话若携带旧 `cursor=1`、pending cursor、resume 或 exhausted 状态，清理旧会话并把 visit/pending cursor 重置为 `0`；同一列表帧内的正常失败重试仍保留当前 pending，不破坏有界重试。
- 好友帮助成功改为强证明：仅按钮消失、soft footer 纹理或 native 方法返回 `True` 均不计数；只接受 durable counter 增长、显式完成回调、明确成功反馈，或农田 ROI 出现足够的维护/作物状态变化。Native 偷菜按钮消失同样需 durable count 或视觉作物变化。
- 真实画面成为页面状态的最高优先级：好友农场同步 `scene_hint=friend`，好友列表同步 `friend-list`，真实自家页同步 `home`，旧持久化 hint 不再覆盖当前画面。
- 回家点击后必须看到真实页面转场；仍停留好友页时保存 retry/next-ts，退避时间到达后最多直点 3 次，随后交给慢速恢复链，避免每轮重复点击和 12 秒热循环。
- 同日分享已经完成但 QQ 联系人弹窗残留时，只执行有界关闭；关闭成功后继续保持分享 blocked，关闭失败进入 backoff，不重新发送。

### RED→GREEN、回归与静态门
- 新增 `tests/test_live_friend_selected_second_reconcile_p0_20260805.py`，现场 P0 五项 `5 / 5 OK`：新会话 cursor=0、按钮消失不算成功、真实好友画面纠正 scene hint、回家失败保存 retry、退避与三次上限。
- 好友专项 `415 / 415 OK`；每日/分享/调度/跨日/计数 `223 / 223 OK`；种植/空地/背包/采集 `296 / 296 OK`；完整回归 `1030 / 1030 OK`。
- `py_compile`、`git diff --check`、hook 编码/运行时身份/导入/完整性 `15 / 15 OK`；`portable/hook.py` 保持无 BOM。

### 性能、备份与正式部署
- 隔离性能门 `120 / 120`：`Responding=False=0`，单核均值 `37.11%`，最大工作集 `516919296`，最大线程 `90`，source 工件冻结且 source=isolated，正式配置未改变，进程树已停止，`failure=null`。
- 正式部署稳定段 `120 / 120`：PID `25560`，`Responding=False=0`，单核均值 `22.64%`，最大工作集 `513855488`，最大线程 `85`；启动时一次 WGC 找窗失败后已自动恢复，稳定段无 Traceback、PrintWindow timeout、回家重试耗尽或帮助未验证热循环。
- 版本 `1.4.35`；source/deploy hook `364EF508424F655F68AAFD81EC3C0B53FAED4FB68F2200D664C01D7108A0CC8C / 1740281 bytes`；正式配置 `5A63582C4A6A9153B51E819DC9D645C44997FD59AA8FE9E0C4A54A5DCA3762A4`。
- 备份 `E:\CV农场助手\backups\v358-perfect-1.4.35-20260805`，含部署前 hook、VERSION、launcher、VBS、完整 `UserData` 570 个文件及 manifest；本轮正式部署仅替换 hook 与 VERSION。
- 随机局部收获、真实好友请求/列表和用户指定每日任务/分享窗口继续作为非阻塞现场签收项；不等待未来事件才推进开发或发布。

## [1.4.34] - 2026-08-05

### 好友入口与第一位好友 P0
- 护主名单模式不再跳过首页“好友求助”直达入口；直达入口仍优先，但 native `True` 只代表点击请求，后续必须看到新鲜好友农场或好友列表画面才算进入成功。
- 对 `check_friend_help_request_entry` 和 `check_friend_icon` 安装运行时转场验证包装；假成功会返回 `False`、清除 pending、开启 30 秒有界冷却并优先普通好友入口，避免每 12–15 秒重复打印和重复点击。
- 好友列表 cursor=0 的第一位好友增加新鲜帧门禁：连续缓存画面不再作为“无动作”证据；偷菜、单偷、帮助和一键务农完成或多帧明确为空后才允许移动到第二位。
- 空好友巡检结束后仍给每日自动捣乱一次独立、耐久、有界的执行机会；失败沿用 full-miss cooldown，不形成第二条热循环。

### RED→GREEN 与完整回归
- 新增 `test_live_friend_entry_p0_20260805.py`、`test_live_first_friend_p0_20260805.py`、`test_live_friend_troublemaker_p0_20260805.py`；三组现场缺口均先取得 RED，再完成 GREEN。
- 好友专项 `410 / 410 OK`；种植/空地/背包专项 `300 / 300 OK`；每日/分享专项 `221 / 221 OK`；完整回归 `1023 / 1023 OK`。
- `python -m py_compile portable\hook.py` 与 `git diff --check` 通过；编码回归测试确认 `hook.py` 继续使用无 BOM 的 ASCII 文件头。

### 性能、备份与部署
- 隔离性能门：权威业务日 `2026-08-05`，完整 PID 树 `120 / 120`，`Responding=False=0`，source hook 冻结且与隔离副本一致，正式配置不变，进程树退出，`failure=null`；汇总证据为 `.analysis\v357-performance-authoritative-20260805.json`。
- 正式启动 burst 后的稳定 120 秒：`Responding=False=0`，单核均值 `32.42%`，最大工作集 `544698368`，最大线程 `85`，进程持续响应。
- 备份 `E:\CV农场助手\backups\v357-perfect-1.4.34-20260805-010859`；正式版本 `1.4.34`；source/deploy hook `FB527AB7...3CB6AF / 1762564 bytes`；正式配置保持 `5A63582C...762A4`。
- 自然跨日每日流程、随机局部收获补种和真实好友求助机会留到对应现场出现时单独签收，不阻塞本轮代码、性能和部署工作。
## [1.4.33] - 2026-08-05

### v356 收获后真实空地与立即补种
- 将“一键务农”和单个收获的确认事件接入同一收获后补种链；保存收获前帧，并以 428×800 基准 24 格中心按实际帧缩放，采集 3 个新鲜帧合并一次性候选。
- 暗坑只在已确认收获上下文中作为候选，必须同时满足局部前后变化；全局普通巡检继续拒绝幼苗、已种作物和刚播种黑土堆，避免重新出现“满地识别出 19 块空地”。
- 支持只随机收获几块：未成熟地块保持原样；`direct_lands` 与剩余 snapshot/recent 队列合并，一次成功只消费本次实际尝试的 1 格或完整 4 格，不再丢邻格或伪造满地。
- 完整田字格才允许 2×2；不完整、灰勾、叉号或无进展后，同轮继续真实 1×1 背包种子，保持背包优先且不进入等级作物购买。

### 每日、跨日与发布门禁
- 非法 `daily_share_time` 现在关闭恢复调度入口，不因异常吞掉而误执行分享。
- 活动实例 `[instance.1.bot] daily_share_time` 从 `09:55` 单行改为 `00:31`；联系人 `1000000001`、精确目标匹配、失败取消和好友开关均保持。
- 跨日 watcher 在计算 `ready_for_restart` 前校验 source/deploy hook 与 config hash；任一错配输出 `stage=artifact-mismatch`、`ready_for_restart=false`，不会启动 launcher。

### 验证、性能与部署
- 种植/空地/背包/性能专项 `305 / 305 OK`；每日/分享 `211 / 211 OK`；好友 `406 / 406 OK`；完整回归 `1009 / 1009 OK`；`py_compile` 与 `git diff --check` 通过。
- 性能 run `v356-20260805-232023104-575eebea724b`：最新 hook 冻结且 source=isolated；完整 PID 树 `120 / 120`；`Responding=False=0`；正式配置前后 hash 不变；进程树退出；`failure=null`。
- 备份 `E:\CV农场助手\backups\v356-perfect-1.4.33-20260805-232655`；正式 hook 已部署为 `4CC1B983...D3CEDF / 1747126 bytes`，正式配置新 hash `5A63582C...762A4`。
- 隐藏启动后的正式 120 秒观察：PID `17476`，`Responding=False=0`，重复分享入口 `0`、直接发送 `0`、PrintWindow 超时 `0`；自然 `2026-08-05 → 2026-08-06` 与随机几块收获后的实机补种仍待最终签收。
### 现场种植 P0
- 使用 `2026-08-05 15:42–15:53` 正式日志和真实截图重新打开种植发布门：两个相邻紫晶空地是真实的两个 1×1 空地，但不构成完整田字格。
- 种子面板现在由空地棋盘门直接判为 `unknown`；它不再把 `before=2` 伪造成 `after=0 / committed=true`。
- 当非完整田字格上的第一张背包卡明确无进展时，只隔离该候选 180 秒，临时关闭 2×2 开关，并在同一背包轮次尝试下一张普通 1×1 卡；退出时恢复用户原开关。
- 同轮降级是有界的一次重试，不递归、不第三次打开背包，并继续保持背包库存优先于等级作物购买。

### RED→GREEN 与回归
- 新增真实 fixture 回归 `tests/test_live_incomplete_quad_backpack_fallback_20260805.py`：修改前两条行为断言失败，修改后 `3 / 3 OK`。
- 空地棋盘门 `10 / 10 OK`；背包 `54 / 54 OK`；2×2 遮罩 `13 / 13 OK`。
- 种植专项 `238 / 238 OK`；每日/分享 `219 / 219 OK`；好友 `397 / 397 OK`。
- 完整回归 `983 / 983 OK`；`py_compile` 与 `git diff --check` 通过。
- 120 秒隔离性能、正式备份部署和当前两个空地的实机闭环仍是本版本发布前的开放门。

## [1.4.32] - 2026-08-05

### 每日、分享与持久化
- 显式以 `Asia/Shanghai` 统一业务日与分享时钟，避免日切与分享窗口沿用隐式本地时区。
- `direct-share` 通过 canonical / `.hook` / 双 CSV 原子镜像收敛状态；每日与分享成功路径写入一致的耐久证据。
- 好友已确认动作新增 `confirmed-proof` append sidecar，保留可追加的确认链，避免重启后把旧残留当作新动作。
- cursor 写入新增 durable journal rollback；嵌套 settings 在流程退出、异常或回滚后恢复原层级配置。

### 种植与性能稳定
- 2×2 种植不满足完整田字格或提交失败时稳定降级为 1×1；背包候选、面板收起和后续巡检保持一致。
- 保持背包与性能路径稳定，避免种植降级或状态恢复把普通轮次重新放大为高成本扫描。

### 验证与证据
- daily/share `200 OK`；完整自动回归 `953 OK`。
- 最新 hook：`4996A7A07F4EFB58934F580671DDAFC10E9DBFD363E055DF3A9CD1F1B0C2002C` / `1629786 bytes`。
- 性能 run `v342-20260805-102051491-ecce9b0cf8b8`：`120/120`；`gate/sampling/process stop/config unchanged`；整机平均约 `2.45%`、P95 约 `6.56%`、`0 not responding`。

### 后续状态
- 正式部署 / 现场 / 重启 / 跨日仍待主线完成；当前条目仅记录代码、测试与性能证据。
## [1.4.31] - 2026-08-04

### 每日与好友防漏
- 修复业务日期变化仍落入 10 秒 metrics 节流的问题：跨日首轮立即刷新 live metrics、canonical/sibling counter 与双 CSV，不让旧日状态延后新日任务。
- 稳定满地 run-cycle 快路径新增每日福利/每日任务 due gate；启用且未完成、未达到当日失败上限时，交还 native scheduler，不延迟任务领取。
- 好友末行 `resume_pending` 和显式 `force_self_cycle` 现在阻断 fast-skip，避免回家后待恢复好友链或强制自家检查被轻量周期覆盖。
- 好友帮助 guard identity 的 primary / `.hook` 镜像原子替换增加有界 sharing-violation 重试，降低成功计数后重启重复点击的窗口。

### 性能与捕获
- `_fake_is_weixin_bound_platform` 不再 stringify 整个运行对象图，只读取有限 protocol/process/platform/mode 字段，消除已定位的单轮秒级热点和无关历史文本误判。
- import hook 按 module identity 去重；循环导入完成前不提前缓存，普通相关导入只 patch 当前模块，`initial/manual/qt-safe-tick` 保留完整维护扫描。
- `gui.*` 当前模块导入时直接安装 `_tick_ui` 节流；WGC normalized frame 缓存和单次连续缓冲复制继续保留。
- 稳定满地完整 `run_cycle` 最多只跳过到 30 秒 native 周期；好友、每日、强制自家、空地、收获播种或画面变化待办会立即退出快路径。

### 验证
- 每日组合 104 项、home/friend 组合 436 项、runtime/performance/daily targeted 67 项通过。
- 完整自动回归：`909 tests in 586.957s / OK`；`py_compile` 与 `git diff --check` 通过。
## [1.4.30] - 2026-08-04

### 好友会话与自家优先调度
- 只有 `board=confirmed + empty=0 + anchors>0` 的新鲜板面才能消费重复的自家强制巡检；画面变化、待播种或未知状态立即恢复完整检查。
- `_run_home_priority_self_pass` 只释放本次临时取得的巡检间隔租约，不结束外层仍在执行的好友链。
- 好友 durable journal 保存 carousel cursor、pending、blocked 和 terminal 状态；明确恢复沿用游标，新会话从第一位开始。

### 验证
- 自家优先租约、好友游标与持久日志完成 RED→GREEN；完整回归 890 项，`py_compile` 与 `git diff --check` 通过。

## [1.4.29] - 2026-08-04

### 合并 2×2 已种植平台与满地性能
- 新增真实满地 fixture：已种植的 2×2 特殊种子会显示为连续紫色大平台，native 空地模板曾把其平滑象限中心 `(276,467)` 误报为 1 块空地，继而进入 pending、反复自家巡检与慢速标签确认。
- 空地候选核心现在同时检查“平滑紫色占比、暖色/深色种植孔缺失、核心亮度稳定、较大范围连续紫色平台”。满足合并平台特征时直接判为已占用，不再武装 home priority，也不再进入慢速面板/标签复核。
- 保留真实紫色空地：普通 1×1 空地中心的暖色/深色种植标记仍是可播种证据，邻格作物叶片不会吞掉空地。

### 验证
- `tests.test_empty_land_merged_purple_crop_regression` 先 RED，再 GREEN；结果从 `empty=1 + pending=True` 变为 `empty=0 + pending=False`。
- 空地/种植目标专项 `153 tests / OK`；种植、空地、背包、捕获与好友组合专项 `555 tests in 205.671s / OK`。
- 完整自动回归：`883 tests in 300.619s / OK`；`py_compile` 与 `git diff --check` 通过。

## [1.4.28] - 2026-08-04

### 2×2 田字格与播种闭环
- 将用户实图中的 4 块空地按坐标拓扑处理：`(130,499)/(189,529)/(247,529)/(218,544)` 分属两个不完整田字格，不再仅凭“空地总数=4”进入 2×2 种子提交；只有同一个完整田字格四格全空才允许四宫格种子。
- 播种后首次读到 `after=0` 但板面仍为 `bypass/unknown` 时，不再提交成功；先收起种子/确认遮罩并抓取第二张新鲜画面，只有 `confirmed + empty=0` 才视为真正种满。
- 背包播种已视觉确认 `remaining=0` 后，立即清除旧 `empty=4` preflight baseline、home priority、visual recheck、forced self 与 recent/stable 空地缓存，阻止 `pending-or-inventory-evidence` 再次打开背包。
- 保留 2×2 灰勾/仅关闭按钮失败后的有界重试、`seed_center` 隔离和同轮 1×1 背包种子降级；不适合 2×2 时不会购买等级作物来替代仍可用的 1×1 背包库存。

### 验证
- 新增 `bypass zero`、收面板后二次 confirmed zero、confirmed zero 清除 stale baseline、生产四点不构成 2×2 四项回归。
- 目标测试 4 项、`tests.test_home_empty_land_priority` 85 项、种植/空地/背包/好友/WGC 组合专项 522 项均通过。
- 完整自动回归：`882 tests in 281.142s / OK`；`py_compile` 与 `git diff --check` 通过。

## [1.4.27] - 2026-08-04

### 背包播种修复
- 修复土地升级/画面缩放后，真实种子工具栏整体下移到 428×800 画面的 y=605..622，但严格面板检测仍只扫描到 y≈594，导致持续出现 `seed-panel-not-confirmed:fresh-frame-not-seed-panel`、4 块空地不播种和高 CPU 重试的问题。
- 种子面板检测新增下移后的四槽数量徽章行，仍要求同一水平行至少 4 个浅色数量徽章，不把普通农场、满地画面或无面板截图误判为背包。
- 新鲜生产 fixture：`tests/fixtures/live-v335-open-seed-panel-four-empty-20260804.png`，徽章数量为 17/36/8/5。

### 验证
- v335 下移种子面板 RED→GREEN；实时种子面板 5 项与背包硬门禁 44 项通过。
- 完整自动回归目标更新为 `878 tests / OK`。

## [1.4.26] - 2026-08-04

### 种植修复
- 修复真实生产中收获后仍有 4 块空地时，种子面板或土地升级遮罩令 post-action 板面门禁变为 `unknown`，但新鲜的零候选读数仍被误记为“播种成功”的问题；该误提交会反复恢复 4 块旧空地并形成高 CPU 播种循环。
- 播种结果现在同时要求“空地数真实下降”和“post-action 板面可用”。板面门禁为 `unknown` 时，即使读到零候选，也按未验证动作处理：不提交成功、不消耗播种队列、保留 home priority，并允许后续种子重试或新鲜板面复核。
- 生产证据保存在 `.analysis/v334-current-game-20260804-2145.png` 与 `tests/fixtures/live-v334-zoomed-dark-seed-mounds-20260804.png`；用户的 `check_interval=12` 和全部配置保持不变。

### 验证
- 新增种子面板/土地升级遮罩下 fresh-zero 误提交 RED→GREEN 回归。
- 完整自动回归目标更新为 `877 tests / OK`。

## [1.4.25] - 2026-08-04

### 性能修复
- 修复稳定满地且好友巡检已锁定后，native 好友分支误判反复设置 `v165` 强制自家巡检，导致同一正常巡检间隔内重复执行截图、空地 CV、OCR 与完整 `process_self_farm` 的问题。
- 新增“已确认无动作自家板面”短时记忆：仅当板面状态为 `confirmed`、空地候选为零、板面锚点和候选签名完全一致、好友空轮已锁定且没有收获/播种/枯地待办时，才在用户原有 `check_interval` 内消费重复的 forced 标志并跳过额外截图/CV。
- 一旦板面签名变化、出现真实空地、收获后播种待办、home priority、枯地/种植配额或正常间隔到期，门禁立即失效并恢复一次新鲜自家检查；用户配置 `check_interval=12` 保持不变。
- WGC 仍保持现有捕获后端和错误保护；OCR 只随实际 self-farm/planting 路径运行，不通过拉长用户巡检间隔掩盖性能问题。

### 验证
- 新增重复 forced self-pass、确认无动作时间戳、成功动作清除 memo、板面签名变化失效与 home priority 失效回归。
- 好友/重复帮助专项 355 项、种植与空地专项 98 项、捕获/WGC/性能专项 41 项通过。
- 完整自动回归 `876 tests / OK`；`py_compile` 与 `git diff --check` 通过。

## [1.4.24] - 2026-08-04

### 修复
- 每日任务 `task_prompt` 初次未命中时，不再只依赖点击前的屏幕点归属。流程先检查任务页黄色领取按钮；仍未进入时向 QQ 农场客户区重发一次任务书入口，等待 1.2 秒并用新鲜帧只重试一次，避免漏做、重复轮询和无限点击。
- 每日任务入口继续保留 v331 的遮挡窗口点击前兜底，并保持任务书默认 2.4 秒有界渲染等待；前台正常点击不双击。
- 已确认帮助的 guard-template 好友身份按日期与实例写入两份每日计数镜像。进程重启后，同一天同一好友的 `soft-help+friend-footer` 残影不再重复点击或重复增加帮助计数；不同好友和真实强帮助按钮不受影响。
- 同日好友身份记录日期滚动自动失效，去重并限制最多 512 条，保留计数、CSV 与其他未知状态字段。

### 验证
- 每日/分享专项 137 项、种植门禁 186 项、捕获/性能门禁 35 项、好友专项 399 项通过。
- 完整自动回归 `873 tests / OK`；`py_compile` 与 `git diff --check` 通过。

## [1.4.23] - 2026-08-04

### 修复
- 每日任务入口在 WGC 可见但 QQ 窗口被其他窗口遮挡时，检测任务入口屏幕点归属；确认原生绝对鼠标点击没有落到农场后，只向 QQ 客户区重发一次任务书入口点击。
- QQ 农场已在前台时保留原生单次点击，避免重复打开/关闭任务弹窗。

### 验证
- 新增遮挡窗口客户区重发与前台不重复点击回归；每日/分享相关 135 项、完整回归 870 项通过。

## [1.4.22] - 2026-08-04

### 修复
- 识别 428×800 实际种子面板的四槽布局（数量徽章 35/36/8/5），背包扫描不再因旧五槽坐标错位反复判定 `fresh-frame-not-seed-panel`。
- 背包候选定位跟随已确认的四槽布局，继续保持首页、满地和无面板画面的严格负例门禁。
- 每日任务入口默认等待预算由 1.2 秒提升为有界 2.4 秒，显式配置不再被 1.5 秒上限截断；分享入口仍保持原快速等待。

### 验证
- 新增真实 v329 种子面板正例、四个背包槽定位和两个无面板负例回归。
- 新增每日任务默认/显式 2.4 秒恢复预算回归；相关子系统 367 项、完整回归 868 项通过。

## [1.4.21] - 2026-08-04

### 每日任务强证明
- 入口红点消失不再单独标记任务成功；只有可见领取动作、原生完成日期或可信完成日志才能形成同日成功记录。
- 缺少强证明时状态保持 pending，下一轮仍可进入 `should/run`；已验证成功则跨重启硬门禁。

### 验证
- 每日任务成功、待执行和重启保持专项 49 项通过。

## [1.4.20] - 2026-08-04

### 启动状态修复
- 启动时复核旧 `task/success/entry-red-dot-cleared` 记录；任务页仍有领取按钮时降级为 `task/pending/task-claim-still-visible`。
- `verified-task-claim-v1` 与可信原生日志继续作为权威成功证明，不受旧红点缓存覆盖。

### 验证
- 旧成功记录、可见领取按钮和持久状态 RED→GREEN 通过。

## [1.4.19] - 2026-08-04

### 分享发送与奖励领取分离
- 精确联系人发送只在单目标、直接确认和窗口关闭均验证后记为成功；奖励领取另存 `share_reward`，互不替代。
- 分享奖励页按当前窗口和 DPI 归一化检测领取/已领取状态，避免把普通绿色分享按钮当作奖励按钮。
- 同日发送成功阻止重复联系人操作，奖励仍可单独补领。

### 验证
- 每日/分享专项 165 项、完整回归 860 项通过；`py_compile` 与 `git diff --check` 通过。

## [1.4.18] - 2026-07-31

### 修复
- 背包优先播种的“已完成”必须有播种动作后的新鲜空地视觉观察，且空地数量下降；`remaining_lands` 仅作诊断，不再单独作为成功证据。
- 缺少播后视觉证据、空地未下降或观察矛盾时，保持自家优先锁、阻止直接买种及好友巡视。
- 2×2 特殊种子失败后的普通背包种子续种不再绕开播后空地复核。
- 每日福利同日持久成功继续作为硬门禁；商城红点不能触发重复领取。
- 更新仅替换程序文件，不覆盖 `UserData`、日志与备份。

### 验证
- 新增 stale 空地计数 + native remaining 误判成功的回归用例。
- 专项 561 项通过；完整回归 615 项通过。
- 补充 `hook.py` 无 UTF-8 BOM 的回归保护，保持代理加载入口的 ASCII 文件头。

## [1.4.17] - 2026-07-31

### 修复
- 修复 `process_friend_farm` 的真值返回误续链：底层处理器返回 `True` 现在仅表示本次调度已结束，不再被当作当前好友仍有可执行动作。只有本次调度新产生的一键动作、已确认的下一好友导航、刚点击且等待转场的好友入口，或真实活动好友链，才允许继续当前链。
- 当前好友没有上述实时证据时，无论底层返回 `True`、`False` 或其他值，均立即锁定本轮空好友巡视并回到自家维护；历史 `pending`、旧动作时间戳和旧导航记录不能再次横向轮询。
- 保留“自家完成有效处理、空地连续两次稳定为 0 后才重新开放一轮好友巡视”的机制，避免在自家仍有播种、施肥、收获或复核工作时离开自家农场。

### 验证
- 新增真值空动作锁定、真值且本轮新动作、新导航、好友入口等待转场的回归用例；好友连续轮询专项 **16 项通过**。
- 好友回家/自家优先专项 **311 项通过**；完整自动回归 `py -3 -m unittest discover -s tests -v` → **609 项通过**；`py -3 -m py_compile portable\hook.py` 通过。
- 部署仅替换程序文件；`UserData` 中配置、每日状态和计数在部署前后执行 SHA-256 校验，防止覆盖用户设置。

## [1.4.16] - 2026-07-31

### 修复
- 修复好友护主的历史动作残留续链：好友卡片上的“一键务农 / 一键收获 / 帮忙”等动作记录现在只可作为点击后的 **8 秒即时交接** 使用，且必须仍是同一张已选卡片。跨巡检、旧时间戳或不匹配卡片一律清空，不能再让好友巡视横向点击后续好友。
- 当前好友经过稳定探测仍没有经过验证的即时动作时，好友链立即标记结束并返回自家；不再把日志里的旧动作标签误认为“本轮仍有动作”。真正刚完成的动作仍只允许检查紧邻下一位好友，紧邻好友无动作即结束本轮巡视。
- 稳定空好友锁、旧版自动重开禁用及“自家有效处理且空地连续两次为 0 才重新开放好友巡视”的规则继续保留。本次更新只替换程序文件，不读取、重置或覆盖 `UserData` 内的等级、休息时段、种植、好友和每日任务状态。

### 验证
- 新增历史动作记录过期阻止横移、即时记录仅允许紧邻一位的回归用例；好友状态机专项 **12 项通过**，好友回家与自家优先专项 **311 项通过**。
- 完整自动回归：`py -3 -m unittest discover -s tests -v` → **605 项通过**；`py -3 -m py_compile portable\hook.py` 与 `git diff --check` 均已执行。

## [1.4.15] - 2026-07-31

### 修复
- 好友护主/好友农场轮询现在以“当前好友是否有经过验证的即时动作”为唯一继续条件：当前好友稳定确认没有一键务农、一键收获、帮忙或其他实际动作时，立即结束好友链、回到自家；不会横向点开后续好友卡片，也不会让历史动作标签或其他好友的残留状态继续驱动巡视。
- 稳定空好友链不再保留旧版约 15 秒的自动重开时间戳。好友巡视进入锁定状态，只有自家完成有效处理后、连续两次稳定确认空地为 0，才重新开放一轮好友巡视。
- 本次更新只替换程序文件；`UserData` 中的休息时段、等级可信状态、种植策略、好友开关、每日分享/福利和计数记录保持原样。

### 验证
- 好友状态机专项覆盖：首位无动作终止、即时动作仅允许紧邻一位、陈旧标签不续链、旧 15 秒定时重开失效，以及自家处理完成后才可重开。

## [1.4.14] - 2026-07-31

### 修复
- 玩家等级改为动态可信状态：首次实时 OCR 与最近可信等级或软锚点相差超过 1 级时，只标记为待确认；必须在 45 秒内获得第二次一致读数后才更新等级和播种策略。近期可信等级不会被更低的单次 OCR 读数倒退覆盖，等级待确认或 OCR 不可用期间继续自家背包播种、空地复核与施肥，但禁止按等级进入商店买种。
- 每日任务失败重试 `3/3` 已改为当日持久封顶。重启时保留合法的 `3/3` 状态，仅裁剪超过上限的异常计数；同一北京日期内不再把耗尽状态重置为 `1/3` 后重复点弹窗。
- 每日分享在首个经过验证的发送或奖励领取证据出现时先落盘，再通知调度器；当天成功记录、原生完成日期和暂停信号均为硬门禁，红点、迟到回调或重启均不会触发第二次分享。
- 延续自家优先链：视觉确认存在空地时先收获、消耗背包种子、复核空地、施肥；在空地未稳定连续两次确认清零前不进入好友巡视，也不因存在新鲜背包种子而购买等级策略作物。
- 更新仍只替换程序文件；`UserData` 中用户的等级软锚点、休息时段、播种、施肥、好友、周期重启、分享及每日状态不被覆盖或回写为默认值。

### 历史回归依据
- 复盘 `2026-07-29.log`：曾出现 `121` 用户被单次读数覆盖为 `120`/`126`、每日任务达到 `3/3` 后重启又从 `1/3` 开始、以及每日分享出现 8 次直接点击并多次将失败计数重置。这三类行为均已有独立回归用例。

### 验证
- 完整自动回归：`python -m unittest discover -s tests -p "test*.py" -v` → **572 项通过**。
- 定向回归：动态等级双样本确认与防倒退 **71 项**；每日状态 **31 项**；分享成功持久化、重复硬门禁与暂停保护 **92 项**；好友回家与每日计数 **309 项**。
- 静态验证：`python -m py_compile portable\hook.py` 与 `git diff --check` 通过。

## [1.4.13] - 2026-07-30

### 修复
- 自家空地优先锁现在可以从 `process_self_farm` 的同模块全局作用域解析原生 `handle_home_planting`；当普通自家巡检没有动作时会直达原生播种入口，而不是在空地未清零时提前转去好友农场。
- 自家空地视觉检测已经确认的坐标会在 120 秒有效窗口内跳过单块“土地名称”慢 OCR；候选仍必须匹配最新空地快照和作物覆盖过滤，避免截图中真实空地因名称 OCR 超时被整批跳过。
- 继续保持实时等级 OCR 规则：OCR 暂时不可用时只继续背包种子和空地维护，不会用历史 `player_level` 选高等级作物或进入商店购买。
- E 盘便携启动、每日状态与用户配置继续保持隔离；更新只替换程序文件，绝不覆盖 `UserData` 中的休息时段、好友、施肥、播种、分享或每日完成记录。

### 验证
- 完整自动回归：`python -m unittest discover -s tests -p "test*.py" -v` → **528 项通过**。
- 定向回归：模块级原生播种入口发现、视觉空地坐标绕过慢名称 OCR、空地优先与好友门禁 **63 项通过**。
- 静态验证：`python -m py_compile portable\hook.py` 与 `git diff --check` 通过。
本项目从 `v1.0.0` 起使用语义化版本号。每个公开版本均对应独立 Git 标签与 Release，便于查看修改历史并回退。

## [1.4.12] - 2026-07-30

### 修复
- 自家空地优先锁在 `process_self_farm` 返回无动作后，不再只重复自家巡检：会在受限对象树中定位原生 `handle_home_planting` 入口并直达播种。空地未稳定连续两次确认清零前，好友巡视仍被拦截。
- 原生播种入口如果位于当前 bot 的嵌套运行时状态中，也会携带正确 owner 调用；直接播种暂时未生效时采用 8 秒短退避后重试，防止每个快速巡检周期反复点击同一面板。
- 便携启动器新增 `UserData\WindowsProfile\Temp`，并向主程序继承 `TEMP`、`TMP`；LocalAppData、RoamingAppData、临时文件、日志、每日状态和用户配置均留在 E 盘应用目录，不回写 C 盘。

### 验证
- 完整自动回归：`python -m unittest discover -s tests -p "test*.py" -v` → **526 项通过**。
- 定向回归：自家空地直达原生播种、嵌套入口发现、失败短退避及到期重试 **4 项通过**；便携 TEMP/TMP 重定向和 PowerShell 语法检查通过。
- 静态验证：`python -m py_compile portable\hook.py` 与 `git diff --check` 通过。

## [1.4.11] - 2026-07-30

### 修复
- 实时等级 OCR 暂时超时或没有稳定结果时，继续执行已打开播种面板中的背包种子播种与空地复核；该保护窗口强制禁止基于旧配置等级选择作物、进入商店或买种，避免 121 级玩家被历史 `125`、`40` 等配置误导到天山雪莲、金花茶等不相关作物。
- 自家空地未完成稳定清零前，持续走“收获 → 背包种子/常规播种 → 动作后空地复核 → 施肥”的自家维护链；2×2 特殊种子失败会隔离该候选并继续正常单格种子，不把一次视觉误点记为有效播种。
- 每日福利、每日任务、每日分享的当日状态和计数通过便携档案保存；已领取、限购耗尽、无任务或已完成均不会因红点或重启重新触发，也不再进入递归重试。
- 便携启动器首次只读导入原有设置，随后将应用的 LocalAppData、RoamingAppData、每日状态及日志重定向到 `UserData\WindowsProfile`；不会回写、规范化或覆盖原 C 盘配置，因此休息时段、好友巡视、施肥、播种、周期重启和分享设置保持用户原值。

### 便携启动
- 新增 `StartFarmAssistant.vbs`，与 `QQFarmCVHelper（双击启动）.lnk` 一起提供隐藏 PowerShell 窗口的一键启动入口；修复 VBS 将 `launcherPath` 当作字面文本传入的引号拼接错误，双击入口现会生成单一、正确加引号的 PowerShell 脚本路径。
- GitHub 更新时保留 `UserData` 即可延续 E 盘中的配置和当天状态；主程序、`hook.py`、`launcher.ps1` 与启动器可直接覆盖更新。

### 验证
- 完整自动回归：`python -m unittest discover -s tests -p "test*.py" -v` → **521 项通过**。
- 定向回归：自家空地优先与好友门禁 **362 项通过**；每日状态 **99 项通过**；启动器迁移、C 盘配置字节保持、VBS 一键启动 **15 项通过**。
- 静态验证：`python -m py_compile portable\hook.py` 与 `git diff --check` 通过。

## [1.4.10] - 2026-07-30

### 修复
- 玩家等级只使用实时 OCR 结果或最近可信实时缓存；OCR 暂时无结果时不再把 `player_level` 配置值当作实时等级。
- 实时等级暂时不可用时，本轮只暂停选作物与买种，6 秒后重试真实 OCR；不会再因旧配置跳转到高等级作物或打开商店买错种子。
- 空地种植识别不再为了速度强制关闭 `enhance_empty_land_detection`，而是严格保留用户已启用的增强空地复核，并继续配合稳定多块空地快照抵御单帧少报。
- 本版不写回或覆盖用户的休息时段、巡视、施肥、播种策略、分享、周期重启等业务配置。

### 验证
- 完整自动回归：`python -m unittest discover -s tests -p "test*.py"` → **490 项通过**。
- 定向回归：实时等级失败不使用配置兜底、等级未就绪不选作物/买种、启用空地增强检测保持、空地稳定快照均已覆盖。
- 静态验证：`python -m py_compile portable\hook.py` 与 `git diff --check` 通过。

## [1.4.9] - 2026-07-30

### 修复
- 自家农场识别到任意空地后，锁定“收获 → 种满空地 → 施肥 → 复核 → 好友巡视”顺序；运行周期入口先执行自家维护，空地连续两次确认清零前不进入好友巡检。
- 空地识别保留稳定的多块快照：一次异常少报不会把尚未种满的农田误判为已完成；背包种子或拖拽动作只有在稳定空地数实际下降时才记为播种成功。
- 好友分支仍停留在自家页面时，首次空转即优先点击右下角“好友”入口；点击后保持 pending 状态，必须等到真实好友列表或好友农场界面出现才允许偷菜、务农或帮助。
- 好友调度和自家维护只恢复运行时内存状态，不重写休息时段、每日分享、施肥、好友巡视、周期重启等用户业务设置。

### 验证
- 完整自动回归：`python -m unittest discover -s tests -p "test*.py"` → **489 项通过**。
- 定向回归：自家空地优先 28 项通过；好友页面误判、封禁跳行、2×2 田字型事务与好友入口确认 305 项通过。
- 静态验证：`python -m py_compile portable\hook.py` 与 `git diff --check` 通过。
## [1.4.8] - 2026-07-29

### 修复
- 启动器只清理配置文件开头的 UTF-8 BOM 三个字节，不解析、不重排、不重写用户业务设置；活动配置在启动、更新和监督重启前后保持字节一致，休息时段、周期重启、每日分享、施肥和等级等开关沿用用户原值。
- 自家农场优先级收敛为“收获 → 播种 → 施肥 → 再次确认空地 → 好友巡检”。空地尚未连续两次确认清零时，好友入口和好友链调度保持阻断；部分白萝卜库存先直接种植，种子模板漏检先走原生铲子/拖拽流程，再决定是否购买。
- 2×2 特殊种子继续遍历 4×6 土地的全部 15 个局部相邻田字型；不足四块、没有合法田字型、对号确认失败或事务重试失败时，只跳过本轮特殊种子，普通单格种子继续处理剩余空地。
- 好友链补强首位、当前护主卡、迟到按钮、同好友先偷取再务农、无动作好友继续向后扫描、封禁返回家和装扮入口门禁；运行时好友参数只在内存恢复工作流所需状态，不写回用户配置。
- 玩家等级首次调用强制实时 OCR，缓存缩短为 120 秒；当 OCR 末位数字混淆但与活动配置处于同一十位区间时，以活动配置作下限修正，避免 `125` 被识别为 `120/121`，旧的 `40` 级配置也不会覆盖实时等级。
- 每日分享仅在“精确目标命中、单联系人已选、直接发送已点击、对话框已关闭”四项证据齐全时持久化成功；同日旧日期字段或红点消失不再冒充发送成功。分享恢复会按原生函数签名传入当前 `game_frame`，已验证成功后迟到的原生失败日志会改写为明确完成提示。
- 施肥拖拽流程记录独立作物上下文，原生拖拽助手输出的“播种”文字会在施肥阶段重标为“施肥”，避免把普通化肥误记成种子。
- 修复单个收获完成日志中的历史乱码哨兵，真实“单个收获连续点击完成”现在会立即追加播种配额，不再等下一轮普通巡检。

### 验证
- 完整自动回归：`python -m unittest discover -s tests -p "test*.py"` → **481 项通过**。
- 静态验证：`python -m py_compile portable\hook.py`、Windows PowerShell 整文件解析与 `git diff --check` 均通过；启动器保持 UTF-8 BOM，配置编码回归证明只删除文件头三个 BOM 字节。
- 实机已有证据（电脑日志时间戳为未来日期 `2026-07-30`，本次未调整系统时间）：实时等级修正为 `125` 并选择对应作物；定向分享完成精确目标输入、单联系人选择、直接发送和对话框关闭，并写入 `verified-direct-contact-send-v2`；好友帮助计数持续增长。
- 真实 2×2 空地现场仍以“出现合法相邻田字型并完成对号确认后，四块空地同时减少”为最终实机验收条件；当前自动回归已覆盖全部 15 个局部位置与失败后普通种子降级。
- 发布构建回归验证便携 ZIP 固定包含 `VERSION`、`README.md`、`版本与更新日志.md` 与按版本生成的 `项目说明.txt`；项目说明直接列出当前版本、完整仓库地址和更新重点。

## [1.4.7] - 2026-07-29

### 修复
- 修复 `freebenefits_flow` 被分享入口、每日状态、分享退避、每日任务退避和平台判断补丁反复交叉包装的问题。每层包装现在继承已有的 `__qqfarm_*_wrapped__` 幂等标记，同一模块被周期扫描时不会继续增加调用深度。
- 商城每日免费福利流程不再因包装层累计触发 `maximum recursion depth exceeded`；即使未来再出现递归异常，也会先写入持久化失败退避，阻止每隔数秒重新进入商城。
- 保留 v1.4.6 的全 24 块土地局部田字型枚举：4×6 地块的全部 15 个相邻 2×2 位置均参与候选，不限农场外侧四角。

### 验证
- 新增 5 项红绿回归，覆盖分享入口与每日状态、微信/QQ 平台判断、分享失败退避、每日任务退避的交叉包装幂等性，以及递归异常先落盘退避；修复前第二轮扫描重复包装或异常后无退避，修复后通过。
- `python -m unittest tests.test_daily_flow_wrapper_idempotence tests.test_daily_flow_status tests.test_share_target_patch -v` 共 86 项通过。

## [1.4.6] - 2026-07-29

### 修复
- 2×2 特殊种子的空地分组改为遍历全部空地四点组合：24 块土地中的任意局部田字型（左上/右上/左下/右下）都可成为候选，不再把整片农场最外侧四角当作唯一组合；横排、竖排、斜排、跨格和外框四角会被几何约束排除。
- 0–3 块空地时完全跳过 2×2 特殊种子，直接继续普通单格种子；4 块以上仅在找到真实相邻田字型时尝试 2×2。
- 2×2 事务连续失败或未找到合法四格时，当轮停用特殊种子分支并重新扫描普通种子，不再终止整轮播种。
- 好友务农按钮连续 3 次点击后仍不消失时，对当前底部好友卡启用 15 秒抑制，阻止同一误报区域每轮重复三击。
- 好友护主列表模式在首位好友的预认证标记缺失时，先用当前底部选中卡与已导入护主模板复核，命中后允许务农，不再固定跳过第一位。
- 护主列表已知有多位候选时，底部轮播遇到中间无动作好友会继续有界向后扫描，不再立即回家而漏掉后续可务农好友。
- 种子栏数量徽章兼容不同纵向比例，用户实图中原先漏掉的中间库存 `48` 已能识别。
- 特定好友皮肤使用“底部绿色选中卡 + 回家按钮 + 务农按钮位置/软分数”联合确认，帮忙后继续复查延迟出现的一键收获。

### 验证
- 4×6 合成农场共有 15 个局部 2×2 位置；回归测试要求全部 15 个都被识别，包含中间位置，并明确排除整片农场外侧四角。
- 最新完整回归 **440 项测试**通过；`python -m py_compile portable\hook.py tests\test_friend_empty_return_home_guard.py` 与 `git diff --check` 通过。
- 生产新进程已加载 `v229-current-card-guard-proof-gap-budget`；实机日志出现当前护主卡身份命中、实际务农动作成功，随后在第 2 个中间无动作好友处继续向后扫描，最终 `moves=3, actions=1`，遇封禁相邻好友被游戏送回家后正常收敛。
- 当前现场仅 3 块空地，可验证“跳过 2×2 并继续普通种子”；真实 2×2 成功种植仍需等待四块相邻田字型空地，以出现对号、点击确认并实际减少四块空地为最终证据。

## [1.4.5] - 2026-07-29

### 修复
- 将原生 `_try_plant_quad_act_seeds` 纳入运行时补丁：2×2 特殊种子严格执行“选择连续 2×2 空地 → 点击 `act_seeds` 特殊种子 → 点击对号确认”事务；首次确认弹层瞬时漏检时，以更低识别阈值和至少 0.35 秒面板稳定时间重试同一事务一次，并在结束后恢复原配置阈值。
- 好友农场底部卡片被窗口裁剪、绿色选中框暂时不可见时，只要“回家”按钮匹配可靠且偷菜/一键务农入口清晰可见，仍判定为可操作好友农场，不再因为缺少底部卡片证据而跳过当前好友。

### 验证
- 新增 3 项红绿回归：裁剪好友画面的可见务农入口、2×2 确认瞬时失败后的单次事务重试、原生 2×2 callable 的运行时补丁接线；修复前 3 项均失败，修复后均通过。
- 好友与播种核心回归 **294 项通过**；完整回归测试 **432 项通过**。
- `python -m py_compile portable\hook.py tests\test_friend_empty_return_home_guard.py`、`git diff --check` 通过。
- v225 已部署到生产目录并由新进程加载；实机好友帮助计数从 750 增长到 751，按钮消失校验通过后才切换底部下一位好友。2×2 特殊种子保留下一次真实库存与连续四格空地场景的持久化结果复核。
- v225 生产进程连续 4 分钟采样 24 次：PID 始终为 `30400`，缺失 0、无响应 0，工作集 416.9–448.5 MB，线程 60–69。

## [1.4.4] - 2026-07-29

### 修复
- 好友列表每次重新打开都从第一行开始；只有第一行明确显示“账号已封禁、无法拜访”时才改点第二行，第二行进入失败时关闭并重开列表，不再继续跳过第三位好友。
- 好友农场底部切换到封禁好友并被游戏送回自家农场时，按正常链路结束处理，不再误判为卡死或继续空巡检。
- 同一好友同时存在偷菜和“一键务农”时保留当前好友进行二次动作探测，两项处理完成后才切换相邻好友。
- 列表点击未确认进入农场时重试同一行；确认好友农场和底部卡片切换完成后才推进游标，避免跳过首位或中间好友。
- 收紧好友农场视觉门禁和底部卡片导航，阻止“回家”低置信度误判以及误入装扮、房屋外观页面。
- 修复作物地块裸土区域被识别为空地的问题；背包中仍有种子时延后商店购买，2×2 特殊种子使用点击种子、选择四格空地和对号确认链路。
- 每日任务、每日分享成功日期和失败退避继续以持久化状态为准，重启或红点误判不再覆盖当天完成状态。
- 关于按钮在第一次点击前替换旧内容，只显示本项目说明和 GitHub 仓库；好友护主/屏蔽截图卡片保持可见，并保留批量全选与一次保存。

### 优化
- 自动捣乱真实成功仍以持久化计数增长为准；一次完全未命中的默认扫描从 5 位好友收敛为 3 位，并保留 90 秒冷却，减少无目标时约 20–30 秒的停留。
- 好友链结束后恢复原始巡检间隔，OpenCV、OCR 和 ONNX Runtime 线程继续受限，降低鼠标、视频和音频卡顿风险。
- 过滤已种植地块的空地误报后，错误背包流程由约 79 秒缩短到约 6 秒内结束。

### 验证
- 完整回归测试 **429 项通过**；`py_compile` 与 `git diff --check` 通过。
- 用户提供的真实好友列表截图中，第一行正常、第二行封禁：封禁模板只命中第二行，访问逻辑实际选择第一行。
- `v223` 连续 12 分钟采样 72 次，PID 始终一致、缺失样本 0、无响应样本 0，工作集约 373–437 MB。
- `v224-troublemaker-bounded-three` 已部署到生产目录，源码与生产 `hook.py` SHA-256 一致；连续 6 分钟 36 个样本 PID 一致、缺失 0、无响应 0。

## [1.4.3] - 2026-07-28

### 修复
- 删除护主名单模式下“处理一位好友后立即结束连续链”的早退分支，多个好友有任务时不再只帮一位便回家。
- 好友列表严格按从上到下选择首个名单匹配行，后续分数更高的候选不会覆盖第一位符合条件的好友。
- 同一好友同时存在偷取和“一键务农”时先完成两项动作，再通过农场底部好友卡继续访问下一位。
- 每次切换到底部相邻好友后重新使用已导入的护主名单头像模板校验身份，避免继承上一位好友的资格。
- 好友链尚未结束时阻止原生回家分支抢先执行；直到遇到首个稳定无偷取、无务农入口的好友后才结束并回家。
- 适配当前 QQ 小程序好友农场真实画面，放宽“回家”和“一键务农”模板的组合阈值，避免按钮清晰可见却被判定为普通页面。
- 删除巡检后再次调用 process_friend_farm 的递归恢复路径，解决 RecursionError 引起的好友页面停留和数秒一次的空巡检循环。
- 每日任务入口打开后若没有出现任务弹窗，按当天已处理记录，清除失败计数与重试退避。
- 每日任务的“入口无弹窗”与用户确认完成记录设为权威状态，后续红点误判不再覆盖当天完成结果。
- 每日任务红点检测改为紧凑连通区域判断，排除农场景物和横向红色条带。

### 优化
- 护主名单模式从自家农场直接打开好友列表，跳过旧的多行串行扫描；由列表预检选择当前页面第一位符合条件的好友。
- 好友连续处理期间将巡检间隔由 2 秒压缩到 0.75 秒，并让每位好友的名单身份只匹配一次，减少列表、进入农场与动作之间的重复等待。
- 优先使用好友农场底部相邻好友卡继续处理，不进入装扮等无关页面，也不反复打开好友列表。

### 验证
- 新增脱敏的 428×800 当前好友农场、护主名单头像行和底部好友卡真实界面回归样本。
- 新增首位优先、多好友连续处理、阻止提前回家、逐好友身份复核、单好友单次匹配、快速巡检间隔和快速列表入口测试。
- 完整测试集共 316 项通过。
- 实机同一好友链已连续记录好友计数 28、29、30；最终构建从好友入口到动作约 10.56 秒，进入好友农场后到识别“一键务农”约 1.4 秒。

## [1.4.2] - 2026-07-28

### 修复
- 好友护主列表已确认目标后，将该资格继承到进入好友农场后的“一键务农”动作。
- 修复进入好友农场后再次调用旧底部门控、丢失列表缓存并错误判定为不可帮助的问题。
- 保留未经过护主列表确认的好友过滤，避免对普通偷菜入口无条件放行。
- 每轮巡检开始时清除上一轮资格，防止资格跨好友残留。
- 将原本约 30–40 秒的无动作串行等待路径收敛为直接执行当前好友动作。

### 验证
- 新增 4 个护主列表资格继承、过期与轮次清理回归测试。
- 完整测试集共 300 项通过。
## [1.4.1] - 2026-07-28

### 修复
- 将“精确联系人已发送成功”设为每日分享的权威成功状态。
- 修复晚到的原生失败结果覆盖已验证发送结果的问题。
- 分享成功后持久化目标、日期与重试状态，重启后不再重复发送。
- 修复分享红点校验误清除当天成功记录的问题。
- QQ 号搜索后精确点击目标联系人左侧选择圆点，再点击可用的发送按钮。
## [1.4.0] - 2026-07-28

### 新增
- 好友农场连续处理链：同一好友先完成偷菜与一键务农，再继续检查后续好友。
- 护主犬好友名单导入、批量勾选、本人头像排除与实例级配置。
- 每日任务、每日分享的持久化状态与红点校验。
- QQ 分享窗口 UI Automation 运行依赖和联系人精确选择基础能力。
- 好友列表、好友农场、回家按钮等视觉模板与真实界面回归样本。

### 优化
- 限制 OpenCV、OCR 与 ONNX Runtime 的并行线程，降低长时间运行时的 CPU 峰值。
- 好友巡检优先处理可操作好友，减少重复打开好友列表与误入装扮页面。
- 自家农场、好友农场和异常页面之间的状态识别与返回路径。
- 启动器保留旧配置，关闭周期性强制重启，并监督意外退出后的恢复。

### 修复
- 好友农场长期停留、只处理一个好友、未完成当前好友动作就回家的问题。
- 自家成熟作物长时间未重新检测的问题。
- 空地识别后未进入播种动作的问题。
- 每日任务和分享在重启后重复执行的问题。
- 界面主题覆盖导致的控件截断、状态栏错位与双击无响应问题。

## [1.3.1] - 2026-07-27

- 修复定向分享重复尝试与错误首选联系人的问题。
- 增加助手意外退出监督与自动恢复。
- 稳定会员权益运行上下文和业务开关。

## [1.3.0] - 2026-07-26

- 增加 OpenCV/OCR 资源限制与运行监控。
- 优化好友连续巡检和自家农场返回链路。
- 完善每日任务、分享与福利状态记录。

## [1.2.0] - 2026-07-26

- 重构 README 与项目视觉素材。
- 精简关于页，只保留项目功能信息。
- 增加用户可编辑的定向分享设置。

## [1.1.0] - 2026-07-25

- 合并旧版配置和便携目录。
- 保留原生浅色界面与控件尺寸。
- 增加启动器、数据迁移和桌面快捷方式支持。

## [1.0.0] - 2026-07-25

- 首个单目录便携版本。
- 集成自家农场、播种、好友巡检和日常功能。
- 建立 GitHub 项目说明与便携交付流程。

<!-- V345-CHANGELOG-START -->
